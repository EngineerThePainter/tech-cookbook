let bestSong = Math.random() > 0.5 ? "Chain of fools" : "Respect";
let firstName = "Whitney";
// Understood that the firstName is string, but call to length()
// as function is not allowed since it's just a number
// firstName.length()
firstName.length

let lastName: string = "Coldface";
// Not allowed - it's string already
// lastName = 21.37; 

let struct = {
    first: "first",
    third: "third"
};

// Not a member
// struct.second

